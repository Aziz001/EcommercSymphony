{% trans_default_domain 'FOSUserBundle' %}
                  <!--overriding fosuser template!-->

<div class="panel-heading">
             <div class="row">
              <div class="col-xs-6">
Change Password             </div>
              
            </div>
            <hr>
          </div>
           <div class="panel-body">
            <div class="row">
              <div class="col-lg-12">
{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" class="btn btn-primary" tabindex="4" class="form-control btn btn-login"/>
    </div>
{{ form_end(form) }}
</div>
            </div>
        </div>